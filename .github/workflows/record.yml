name: Agent - record a PR's tests

# Label a PR `record` and this records the tests the branch added, then links the
# clips. Deliberately NOT part of agent.yml: a recording is optional and the pull
# request it describes is not, so it runs once the work is already safe on a
# branch. That separation was bought the hard way — a hung ffmpeg once burned an
# agent run's remaining minutes and lost a finished implementation before it
# could be pushed.
#
# Everything that used to make this workflow 13 steps now lives in twd-cli: the
# ffmpeg install and its capability probe, recording only the changed tests,
# aborting on a dead ffmpeg, surfacing its stderr, and transcoding to something
# any player opens. What is left here is what is genuinely this repo's business:
# when to run, and what to say afterwards.
on:
  pull_request:
    types: [labeled]

concurrency:
  group: record-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  record:
    if: github.event.label.name == 'record'
    runs-on: ubuntu-latest
    # Belt, not workaround: the hang class is fixed in the CLI, but a recording
    # must never be able to cost a caller more than a recording.
    timeout-minutes: 15
    permissions:
      contents: read
      pull-requests: write # comment the link, drop the label
    env:
      GH_TOKEN: ${{ github.token }}

    steps:
      # The PR's head, not the merge commit: the point is to see the tests this
      # branch built. fetch-depth 0 because --changed-since needs history, and a
      # depth-1 clone does not contain the base commit at all.
      - name: Checkout the PR head
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # The action assumes the app is already served at the url in
      # twd.config.json — the same contract the `run` action has.
      - name: Start Vite dev server
        run: |
          nohup npm run dev > vite.log 2>&1 &
          npx wait-on http://localhost:5173
        env:
          CI: true

      # Pinned to a commit rather than @main: the action decides how a recording
      # comes out, so it must not change under an unchanged repo.
      - name: Record the tests this branch added
        id: rec
        uses: BRIKEV/twd-cli/.github/actions/record@5d4f8c7118efe2435c38f8098f32b1374d1092a1
        with:
          changed-since: ${{ github.event.pull_request.base.sha }}
          artifact-name: twd-recording-pr-${{ github.event.pull_request.number }}

      # Best-effort: a fork PR gets a read-only token and cannot comment. Zero
      # clips is a normal outcome rather than a failure — a branch that changed no
      # tests has nothing to record.
      - name: Comment the link
        if: always()
        continue-on-error: true
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          CLIPS: ${{ steps.rec.outputs.clip-count }}
          VIDEO_URL: ${{ steps.rec.outputs.artifact-url }}
        run: |
          if [ "${CLIPS:-0}" = "0" ]; then
            gh pr comment "$PR_NUMBER" --body "Nothing to record: this branch added no TWD tests."
          else
            gh pr comment "$PR_NUMBER" --body "Recording: ${CLIPS} clip(s), one per test this branch added — [download the artifact](${VIDEO_URL}) and unzip."
          fi

      # Dropping the label makes a retry one click instead of remove-then-add.
      - name: Drop the label
        if: always()
        continue-on-error: true
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: gh pr edit "$PR_NUMBER" --remove-label record
